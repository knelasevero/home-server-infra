[all:vars]
ansible_connection=ssh
ansible_user=${ansible_user}

[common]
${server_fqdn}

[libretranslate]
${server_fqdn}

[nextcloud]
${server_fqdn}

[onetimesecret]
${server_fqdn}

[reverse-proxy]
${server_fqdn}